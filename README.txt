Installation:

Setup a virtual environment with Python 3.7
pip install -r requirements.txt

To run:
jupyter lab finalproj.ipynb

A jupyter lab environment should open in the browser. Run all cells starting from the top, up to the message in the notebook that says roughly "this is the end of the current code."